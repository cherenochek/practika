<?php
if ($users->count() > 0) {
     foreach ($users as $user) { 
            
                 echo $user->id; 
                 echo $user->name; 
                 echo $user->email; 
          
         } 
    
}